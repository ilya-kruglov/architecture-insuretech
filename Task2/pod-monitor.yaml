apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: scaletest-podmonitor
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: scaletest-app
  podMetricsEndpoints:
    - port: http
      path: /metrics
      interval: 30s